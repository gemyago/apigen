openapi: "3.0.0"
info:
  version: 0.0.1
  title: |
    Set of APIs to test error handling
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
servers:
  - url: http://localhost:8080/v1
paths:
  /error-handling/parsing-errors:
    get:
      summary: API to simulate and test parsing errors
      operationId: parsingErrors
      tags:
        - numeric-types
      parameters:
        - name: requiredQuery1
          in: query
          required: true
          schema:
            type: number
        - name: requiredQuery2
          in: query
          required: true
          schema:
            type: number
      responses:
        '204':
          description: Request succeeded